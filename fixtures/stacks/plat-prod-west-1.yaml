apiVersion: atmos.cloudposse.com/v1
kind: Stack
metadata:
  name: plat-prod-secondary
  labels:
    environment: prod
    <<: *secondary-region
    team: platform
spec:
  components:
    terraform:
      vpc:
        vars:
          <<:
            - *secondary-region
            - *vpc-defaults
            - *vpc-overrides
          ipv4_primary_cidr_block: 10.2.1.0/16
